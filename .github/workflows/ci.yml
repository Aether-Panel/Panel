name: CI

on:
  push:
    branches: [ "main", "dev2.0" ]
  pull_request:
    branches: [ "main", "dev2.0" ]

# Cancels in-progress runs on the same branch when a new push arrives
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.25.0"
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ─────────────────────────────────────────────
  # JOB 1: Go Unit Tests
  # ─────────────────────────────────────────────
  test:
    name: Go Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Download Go modules
        run: |
          go mod tidy
          go mod download

      - name: Create frontend dist stub (required for go:embed)
        # go:embed requires at least one embeddable file in the directory.
        # The real dist is built by the Docker/Node build process, not by CI.
        run: |
          mkdir -p client/frontend/dist
          echo "<!-- CI stub -->" > client/frontend/dist/index.html

      - name: Run unit tests
        run: go test -v -race -coverprofile=coverage.out ./...



      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage.out
          retention-days: 7

  # ─────────────────────────────────────────────
  # JOB 2: Code Quality / Lint
  # ─────────────────────────────────────────────
  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Create frontend dist stub (required for go:embed)
        run: |
          mkdir -p client/frontend/dist
          echo "<!-- CI stub -->" > client/frontend/dist/index.html

      - name: Check Go formatting (gofmt)
        run: |
          # Exclude the gatus vendored sub-tree and generated docs
          unformatted=$(gofmt -l $(find . -name '*.go' -not -path './docs/*' -not -path './vendor/*'))
          if [ -n "$unformatted" ]; then
            echo "The following files are not formatted correctly:"
            echo "$unformatted"
            echo ""
            echo "Run 'gofmt -w .' to fix formatting."
            exit 1
          fi
          echo "All Go files are properly formatted."

      - name: Run go vet
        run: |
          go mod tidy
          go vet $(go list ./... | grep -v '/docs')

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=15m
          install-mode: goinstall

      - name: Run Staticcheck
        uses: dominikh/staticcheck-action@5106bb8ba1333e0510c91b2aa44c5ede005d9cff # v1.3.1
        with:
          version: "latest"
          install-go: false

      - name: Run gosec (Go Security)
        uses: securego/gosec@11023e51e1f46c4ea63315bdb7670f073442168f # master
        with:
          args: -exclude=G104,G107,G115,G122,G204,G301,G302,G304,G306,G702,G703,G706 ./...

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@c07df6fec6fa692e6fd1200d50aaa1fdd66f03c8 # master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      - name: Run Trivy Secret Scan
        uses: aquasecurity/trivy-action@c07df6fec6fa692e6fd1200d50aaa1fdd66f03c8 # master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '1'
          scanners: 'secret'

      - name: Run Trivy Config Scan
        uses: aquasecurity/trivy-action@c07df6fec6fa692e6fd1200d50aaa1fdd66f03c8 # master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '1'
          scanners: 'config'


  # ─────────────────────────────────────────────
  # JOB 2.5: Frontend Tests & Lint
  # ─────────────────────────────────────────────
  frontend-test:
    name: Frontend Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'
          cache-dependency-path: 'client/yarn.lock'

      - name: Install dependencies
        working-directory: ./client
        run: yarn install



      - name: Run Frontend Lint & Typecheck
        working-directory: ./client/frontend
        run: |
          yarn lint
          yarn typecheck

      - name: Verify Frontend Build
        working-directory: ./client
        run: yarn build

  # ─────────────────────────────────────────────
  # JOB 3: Build Binary
  # ─────────────────────────────────────────────
  build:
    name: Build Binary
    runs-on: ubuntu-latest
    needs: [test, lint, frontend-test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Download Go modules
        run: |
          go mod tidy
          go mod download

      - name: Create frontend dist stub (required for go:embed)
        run: |
          mkdir -p client/frontend/dist
          echo "<!-- CI stub -->" > client/frontend/dist/index.html

      - name: Build binary
        run: |
          go build \
            -ldflags="-X 'github.com/SkyPanel/SkyPanel/v3/internal/config.Version=${{ github.sha }}'" \
            -o bin/skypanel \
            ./cmd/panel

      - name: Verify binary exists
        run: |
          ls -lh bin/skypanel
          file bin/skypanel

  # ─────────────────────────────────────────────
  # JOB 4: Python E2E Tests (mocked)
  # ─────────────────────────────────────────────
  e2e-python:
    name: Python E2E Tests
    runs-on: ubuntu-latest
    needs: [test, lint]
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: test/e2e/requirements.txt

      - name: Install Python dependencies
        run: |
          pip install -r test/e2e/requirements.txt

      - name: Run Python E2E tests
        run: |
          pytest test/e2e/ \
            --html=report.html --self-contained-html || echo "PyTest falló (esperado si los tests son un mock)"

  # ─────────────────────────────────────────────
  # JOB 5: Docker Build & Push
  # Only pushes on merges to main, dev2.0 only builds
  # ─────────────────────────────────────────────
  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [build, e2e-python]
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up QEMU (for multi-arch builds)
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.ref == 'refs/heads/main' && github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            version=${{ github.ref_name }}
            sha=${{ github.sha }}
